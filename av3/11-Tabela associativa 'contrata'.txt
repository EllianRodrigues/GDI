CREATE TABLE Contrata (
    CPF_h INT,
    Reserva_id INT,
    Numero_quarto INT,
    PRIMARY KEY (CPF_h, Reserva_id, Numero_quarto),
    FOREIGN KEY (CPF_h) REFERENCES Hospedes(CPF_p),
    FOREIGN KEY (Reserva_id) REFERENCES Reserva(Reserva_id),
    FOREIGN KEY (Numero_quarto) REFERENCES Quarto(Numero_quarto)
);

INSERT INTO Contrata (CPF_h, Reserva_id, Numero_quarto)
VALUES (444444444, 1, 103);

INSERT INTO Contrata (CPF_h, Reserva_id, Numero_quarto)
VALUES (555555555, 2, 102);

SELECT * FROM Contrata;